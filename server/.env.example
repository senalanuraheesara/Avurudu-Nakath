# MongoDB connection string (MongoDB Atlas in AWS-friendly region, or DocumentDB)
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/avurudu

# Port (App Runner / ECS often inject PORT — default 4000)
PORT=4000
